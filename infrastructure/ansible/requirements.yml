---
# Collections required by the playbooks and roles in this directory.
# Install with: ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.general
    version: ">=6.0.0"
  - name: community.mongodb
    version: ">=1.5.0"
  - name: ansible.posix
    version: ">=1.4.0"
